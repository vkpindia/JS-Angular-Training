/* 
 * Steps to compile ts fie

1. Create a .ts file
2. then target the folder in terminal where file is available
3. then use the command 

tsc filename.ts

4. Ts file will compile and create a new file with same name but with .js 
extension
5. Then we can use transpiled js file in html code by using <script> tag
*/

function showMessage(){
    console.log("Welcome TypeScript Programing");
}